---
name: draft-report
description: Turn a one-line task into a fully-filled issue report for ShayNeeo/grab-the-future-hackathon and write it to .claude/drafts/draft-issue.md. No issue templates exist — always uses the fallback markdown structure. Actors: elderly Vietnamese users (end-user) or Developers/maintainers (infra/refactor).
---

> Generated by `specialize-skill` from the general `draft-report` skill for
> `ShayNeeo/grab-the-future-hackathon`. Concrete to THIS repo. Re-run
> `specialize-skill draft-report` after the general skill changes.

# Draft Report — ShayNeeo/grab-the-future-hackathon

Turn a one-line task into a fully-filled report, then write it to
`.claude/drafts/draft-issue.md` so `create-issue` can open it on GitHub.

## Step 1 — Pick the template

**This repo has no issue templates** (`.github/ISSUE_TEMPLATE/` is empty, no
GitLab equivalents). Always fall back to the standard markdown structure below —
do not invent template paths.

| Title tag | Inferred kind | Fallback sections |
|---|---|---|
| `[Feature]` | feature | Summary, Business Goal, Proposed Behavior, Acceptance Criteria, Out of Scope, Dependencies |
| `[Task]` / `[Chore]` | implementation task | Summary, Context, Affected files, Proposed behavior, Acceptance criteria, Out of Scope, Dependencies |
| `[Bug]` | bug report | Summary, Steps to Reproduce, Expected vs Actual, Affected files, Fix approach, Acceptance criteria |
| PR / change already made | PR body | Summary, What Changed checkboxes, Verification, Breaking Changes, Follow-Up |

## Step 2 — Gather grounding (do not fabricate)

1. **No CLAUDE.md or docs/ in this repo.** Rely entirely on codebase inspection —
   no canonical doc paths to cite. Delete any `docs/...` reference lines rather
   than leaving them.
2. **Actors in this project:**
   - End-user-facing changes → **"Elderly Vietnamese users (bà/ông dùng app)"**
   - Infra / refactor / backend → **"Developers / maintainers (no end-user-facing change)"**
3. **Inspect the code** by searching for keywords from the task title to find the
   relevant files, then read only those files so every claim about current state
   is grounded. If no paths can be identified confidently, say so.
4. **Check git** (`git log --oneline -10`, `git diff --stat`) when drafting a PR.
5. **Code wins over memory.** When a file you read contradicts something assumed,
   call out the discrepancy in an "Inconsistencies found" note.

## Step 3 — Fill every section

Replace each placeholder with concrete prose. Rules:

- **Summary** — 2–5 sentences: what's broken/missing and what changes.
- **Business Goal / Why** — tie it to ScamShield's purpose: protecting elderly
  Vietnamese users from scams. Be specific to *this* task.
- **Actor** — pick from the vocabulary in Step 2.
- **Affected files** — table of `file | current problem`. Use clickable relative
  paths (e.g. `[lib/ui/screens/foo.dart](lib/ui/screens/foo.dart)`). Only list
  files you've verified exist.
- **Proposed behavior** — numbered, concrete steps. For a refactor, state
  source→target, parity requirement, and sequencing.
- **Acceptance criteria** — specific, checkable. Tick boxes that apply; add
  task-specific ones.
- **Out of scope** — name plausible adjacent work this task must NOT absorb.
- **Dependencies / Blockers** — real ones. Write "None known" if genuinely none.

## Step 4 — Write the draft file

Write to `.claude/drafts/draft-issue.md`. Create `.claude/drafts/` if needed.
Use this exact structure:

```
---
title: "[Tag] <concrete title>"
kind: issue
template: fallback
labels: [<inferred>]
---

<fully-filled body — no <!-- --> placeholders, no dangling docs/... refs>
```

**Label inference** (only labels that exist on this repo):

| Title tag | Label |
|---|---|
| `[Bug]` | `bug` |
| `[Feature]` | `enhancement` |
| `[Task]` / `[Chore]` | *(skip — `task` label doesn't exist)* |
| `[Docs]` | `documentation` |
| anything else | *(skip)* |

## Step 5 — Deliver

1. Confirm the file written with its path.
2. Print the filled report in a fenced ` ```markdown ` block.
3. Offer follow-ups (do not run without asking):
   - Run `create-issue` to open it on GitHub.
   - Copy it into a task doc (no canonical docs/ path in this repo — user decides).

## Quality bar

- File written to `.claude/drafts/draft-issue.md` with valid frontmatter.
- Zero `<!-- -->` comments and zero `docs/...` ellipsis placeholders in the body.
- Every referenced path verified to exist in the repo.
- Labels field contains only labels that exist on `ShayNeeo/grab-the-future-hackathon`.
- Content is specific enough that a teammate could start work from it alone.
